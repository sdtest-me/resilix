import{detectContradictions}from"./contradiction_engine.js";import{mapToVMEME}from"./spiral_mapper.js";export function runPipeline(rawSignals){const signals=rawSignals.map((s,i)=>({id:`sig_${i}`,type:s.type,source:s.source,weight:s.weight??0.5,source_confidence:s.confidence??"medium"}));const derived_patterns=signals.filter(s=>s.weight>0.6).map((s,i)=>({id:`pat_${i}`,type:"operational_signal",derived_from_signals:[s.id],inference_confidence:s.source_confidence,reasoning:"High-weight signal"}));const hypotheses=derived_patterns.map((p,i)=>({id:`hyp_${i}`,type:"dependency_risk",derived_from_patterns:[p.id],confidence:p.inference_confidence,reasoning:"Pattern-based inference"}));const contradictions=detectContradictions({signals});const vMEME=mapToVMEME({signals});return{signals,derived_patterns,hypotheses,contradictions,vMEME}}