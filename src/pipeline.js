export function runPipeline(rawSignals){const signals=rawSignals.map((s,i)=>({id:\,type:s.type,source:s.source,weight:s.weight??0.5,source_confidence:s.confidence??"medium"}));const derived_patterns=signals.filter(s=>s.weight>0.6).map((s,i)=>({id:\,type:"operational_signal",derived_from_signals:[s.id],inference_confidence:s.source_confidence,reasoning:"High-weight signal\`}));const hypotheses=derived_patterns.map((p,i)=>({id:\,type:"dependency_risk",derived_from_patterns:[p.id],confidence:p.inference_confidence,reasoning:"Pattern-based inference"}));return{signals,derived_patterns,hypotheses}}