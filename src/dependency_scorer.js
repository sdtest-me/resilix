export function calcDependencyRisk(evidence){const signals=evidence.signals||[];const leadership=signals.filter(s=>s.type.includes("leadership")||s.type.includes("succession"));const weight=leadership.reduce((sum,s)=>sum+(s.weight||0.5),0);const confidence=leadership.length>2?"high":leadership.length>0?"medium":"low";const score=Math.min(100,Math.round(weight*100/leadership.length))||0;return{score,confidence,drivers:leadership.map(s=>s.type),derived_from_signals:leadership.map(s=>s.id),reasoning:leadership.length>0?"Key-person concentration detected":"No leadership risk signals"}