export function detectContradictions(evidence){const res=[];const sigs=evidence.signals||[];for(let i=0;i<sigs.length;i++){for(let j=i+1;j<sigs.length;j++){const a=sigs[i],b=sigs[j];if(a.type&&b.type&&a.type!==b.type&&a.weight>0.6&&b.weight>0.6){res.push({id:`ctr_${i}_${j}`,between:[a.type,b.type],severity:"medium",derived_from_signals:[a.id,b.id],reasoning:`High-weight signals conflict: ${a.type} vs ${b.type}`})}}}return res}